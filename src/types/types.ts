export type ProductType = {
  id: number;
  title: string;
  description: string;
  image: string;
  price: string;
};

export type CartPropsType = {
  id: number;
  title: string;
  price: string;
  image: string;
  quantity: number;
};
